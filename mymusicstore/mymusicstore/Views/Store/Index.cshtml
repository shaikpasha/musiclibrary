@model IEnumerable<mymusicstore.Models.Genre>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
<p> Browse genres</p>
<ul>
    @foreach (var genre in Model)
    {
        <li>@Html.ActionLink(genre.Name, "Browse", new { id = genre.Name }) </li>
    }
</ul>